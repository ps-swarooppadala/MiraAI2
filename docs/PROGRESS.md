# Build Progress

## Done
- **Phase 0: Walking skeleton.** CameraX → MediaPipe PoseLandmarker (CPU delegate) → one hardcoded right-elbow angle check → hardcoded TTS line via system TextToSpeech. Manual on-device smoke test passed 2026-08-29 on vivo I2501 (Android 16). **Sustained FPS: 25–31 (avg 29–30), exceeds 15–20 FPS target.** Commit: `phase-0-walking-skeleton`.
- **Phase 1: Assessor core (Warrior II rules), pure Kotlin.** `WarriorIIAssessor.assess(frame, frontLeg)` implements the Section 10.1 verdict contract (single `verdictCode`, `hasCriticalIssue`, `confidence`) against a minimal pure-Kotlin pose model (`PoseModel.kt`: `BodyJoint`, `Landmark`, `PoseFrame`, `Side`) built on the existing `Point2D`/`angleDegrees` primitives from Phase 0's `AngleMath.kt`. `HoldTimer`/`HoldTimerState` implement the Section 8.3 hold-timer + Section 8.2 pause contract, including the explicit (non-placeholder) 2-second clean-tail. 24/24 JUnit tests pass (4 existing `AngleMathTest` + 9 `HoldTimerTest` + 11 `WarriorIIAssessorTest`). Commit: `Phase 1: Warrior II assessor core`.

  **Known deviation from feature-spec.md/build-architecture.md, flagged for a future doc pass:** both docs specify package `com.mira.ai`, but Phase 0 (already committed, on-device tested) used `com.mira.miraai` throughout (`app/build.gradle.kts` namespace/applicationId + every existing source file). Phase 1 followed the existing `com.mira.miraai` to avoid forking the package tree or renaming already-shipped Phase 0 code outside this phase's scope. Docs should be corrected to `com.mira.miraai`, or the app renamed to `com.mira.ai`, in a dedicated pass — not silently by whichever phase notices next.

  **Placeholder constants needing on-device tuning** (`WarriorIIThresholds.kt`, all explicitly commented as placeholders): `FRONT_KNEE_TARGET_DEG` (90°), `FRONT_KNEE_TOLERANCE_DEG` (15°), `BACK_LEG_STRAIGHT_MIN_DEG` (160°), `ARM_LEVEL_TOLERANCE` (0.05, normalized y), `TORSO_UPRIGHT_TOLERANCE` (0.05, normalized x), `FRONT_KNEE_PAST_ANKLE_TOLERANCE` (0.08, normalized x), `MIN_LANDMARK_VISIBILITY` (0.5). None are sourced from feature-spec.md — Section 8.3 explicitly leaves these open pending real camera data.

  **Scope notes for later phases:**
  - `PoseModel.kt` (`BodyJoint`/`Landmark`/`PoseFrame`) lives in `assessor/` for now since Phase 3's real `PoseEstimator` interface (build-architecture.md Section 2: `interface PoseEstimator { fun estimate(frame: ImageProxy): PoseFrame }`) doesn't exist yet — only a concrete MediaPipe-coupled `PoseEstimator` class does (`perception/PoseEstimator.kt`, Android-coupled, not the pure-Kotlin contract). Phase 3 may want to relocate/adapt this pose model into `perception/`.
  - `ElbowCheck.kt` (Phase 0's placeholder single-joint check) is left untouched — `MainActivity.kt` still calls it, and rewiring the camera pipeline onto `WarriorIIAssessor` is explicitly Phase 4's job ("Real camera pipeline wired to Phase 1+2"), not Phase 1's. Remove `ElbowCheck.kt` when Phase 4 does that rewiring.
  - `BOTH`-side sequencing and rep-counting are Coach Agent/Routine Sequencer territory (Phase 2), not covered here.

- **Phase 2: Coach Agent state machine, pure Kotlin.** `CoachAgent.tick(WarriorIIVerdict)` (`com.mira.miraai.agent`) implements the guardrail rules named in build-architecture.md Section 3 (cue cooldown, escalate-only-on-persistence, confirm improvement, safety overrides everything, never coach below confidence threshold). Fed by a `Clock` seam and a fake scripted `WarriorIIVerdict` stream in tests — no camera, no LLM. 18/18 new JUnit tests pass (`CoachAgentTest.kt`); 42/42 total across the suite.

  **Doc gap found, not resolved by this phase:** feature-spec.md Section 10.2 says "8 hard guardrail rules unchanged" but only 6 are named anywhere in either doc (build-architecture.md:120-122): one cue per cooldown, escalate only on persistence, confirm improvement, safety overrides everything, never coach below confidence threshold, never diagnose. "Never diagnose" is a Mouth/template-language constraint, not agent state, so it isn't modeled in `CoachAgent`. The other 2 rules aren't written down anywhere found — needs a doc pass to either name them or correct "8" to "6".

  **Ordering decisions made where the spec is silent** (see `CoachAgent.kt` doc comment): pause outranks safety override (a paused workout never speaks or mutates state, no exception carved out in Section 8.2); safety cues bypass both the cooldown and the confidence gate, confirm-improvement cues bypass only the cooldown (not the confidence gate — don't praise on garbage tracking data).

  **Placeholder constants needing on-device tuning** (`CoachAgentThresholds.kt`, all explicitly commented as placeholders): `CUE_COOLDOWN_MS` (8000), `SAFETY_COOLDOWN_MS` (3000), `MIN_CONFIDENCE_TO_COACH` (0.6), `ESCALATION_THRESHOLD` (2 uncorrected cues). None are sourced from feature-spec.md.

  **Scope notes for later phases:**
  - `CoachAgent` is coupled to `WarriorIIVerdict` directly (Phase 1's only concrete verdict type) rather than a generic `Verdict` interface. If/when a second pose's assessor produces its own verdict type (Phase 13+), extract a shared interface at that point rather than guessing its shape now.
  - Confirm/escalation tracking treats safety cues as a separate track from regular issue cues (a safety interruption doesn't reset or advance the regular-issue persistence count). Not spec-mandated, just the simplest correct reading.
  - Routine sequencing, `WorkoutSessionState` wiring, and the Freestyle `ActionSchema` path are out of scope for Phase 2 — that's Phase 3+ (device abstraction) and Phase 8 (Freestyle) per the phase table.

- **Phase 3: Device abstraction — interfaces + `devPhone`/`iqoo` flavors + DI modules.** Added the four interfaces from build-architecture.md Section 2 (`perception.PoseEstimator`, `voice.LLMProvider`, `voice.TTSProvider`, `voice.STTProvider`), a real `SystemTTSProvider` (Android `TextToSpeech`, both flavors — Piper is still Phase 7), and placeholder `StubLLMProvider`/`StubSTTProvider` (real classifier/ASR wiring is Phase 8). Added Gradle `flavorDimensions += "device"` with `devPhone`/`iqoo` product flavors, each with its own Koin module (`di/AiModule.kt`, one file per flavor source set, same FQN — only the pose delegate differs today: `Delegate.CPU` vs `Delegate.GPU`) wired up via a new `MiraApplication` that calls `startKoin`. Added `Fake*` implementations of all four interfaces (`perception.FakePoseEstimator`, `voice.FakeLLMProvider/FakeTTSProvider/FakeSTTProvider`) plus contract tests. 50/50 JUnit tests pass on both `testDevPhoneDebugUnitTest` and `testIqooDebugUnitTest`.

  **Relocation done this phase (per Phase 1's note):** `PoseModel.kt` (`BodyJoint`/`Landmark`/`PoseFrame`/`Side`) moved from `assessor/` to `perception/`, since `PoseFrame` is now the interface's return type. `WarriorIIAssessor.kt`/`WarriorIIAssessorTest.kt` updated to import from `perception` — no other behavior changed.

  **Known deviation, flagged not silently patched:** the concrete pose estimator (renamed `MediaPipePoseEstimator`, was `PoseEstimator`) does **not** implement the new `perception.PoseEstimator` interface. The interface is synchronous (`estimate(frame): PoseFrame`) per the spec doc; the concrete class uses MediaPipe's `LIVE_STREAM` mode (async, callback-based) because that's what Phase 0 measured 25-31 FPS with on-device. `MediaPipePoseEstimator` is bound in Koin only via its delegate config (`named("poseDelegate")`), not as the `PoseEstimator` interface. Reconciling this (async interface variant, or a synchronous adapter) is explicitly Phase 4's job when the camera pipeline gets rewired onto the Assessor — not Phase 3's, per CLAUDE.md's "do not re-architect completed phases."

  **Minimal data-model types pulled forward, not full-featured:** `agent/freestyle/FreestyleTypes.kt` (`ActionSchema`, `AgentPrompt`, `AgentResponse`, `SessionContext`) and `memory/Fact.kt` were added now, ahead of their real phases (8 and 9/10), only because `LLMProvider.complete()` needs their shapes to compile. No classifier, repository, or consolidation logic exists yet — don't mistake these for Phase 8/10 being started early.

  **Toolchain note:** `mockito-core`/`mockito-kotlin` versions in `libs.versions.toml` were pinned to 5.19.0/5.4.0 — older Mockito (5.14 and below) fails on this machine's JDK 25 (Byte Buddy doesn't support it) when mocking `ImageProxy` in `FakePoseEstimatorTest`. If tests fail with `ByteBuddy ... Java 25 ... not supported`, bump `mockitoCore` further rather than downgrading the JDK.

- **Phase 4: Real camera pipeline wired to Phase 1+2, `devPhone` flavor.** `MediaPipePoseEstimator` now implements `perception.PoseEstimator` (resolving Phase 3's flagged deviation) via `estimate(frame: ImageProxy, onResult: (PoseFrame) -> Unit)` — an async contract, not the originally sketched synchronous one, because MediaPipe's `LIVE_STREAM` mode only supports a registered result listener (needed for Phase 0's measured FPS). Added the `PoseLandmarkerResult` → `PoseFrame` mapping (`MediaPipePoseEstimator.kt`'s `toPoseFrame()`) using a full 10-joint `PoseLandmarkIndex` (was 3 joints, right-arm-only, left over from Phase 0). `PoseEstimator` is now bound in both flavors' `aiModule` and injected into `MainActivity` via Koin (`by inject()`), replacing direct construction. `MainActivity` now runs the real pipeline end to end: camera frame → `PoseEstimator.estimate` → `WarriorIIAssessor.assess` → `CoachAgent.tick` → a `CueTemplates` phrase → `SpeechCoach.speak`. Removed Phase 0's `ElbowCheck.kt` and its two redundant `AngleMathTest` cases (same angle math already covered by the two `angleDegrees` tests). 48/48 JUnit tests pass on both `testDevPhoneDebugUnitTest` and `testIqooDebugUnitTest` (50 minus the 2 removed `ElbowCheck` tests).

  **Not done this phase, explicitly deferred:** the on-device golden landmark fixture + regression test named in build-architecture.md's Phase 4 test-strategy cell ("On-device integration test, golden landmark fixtures") was **not** recorded — it requires physically operating a devPhone camera, which isn't available in this environment. No fixture/eval-harness code exists yet. Whoever picks this up next with device access should: (1) run the app on-device in good Warrior II form and 2-3 common-mistake variants (e.g. front knee past ankle, torso leaning, back leg bent), (2) log the resulting `PoseFrame` (or raw `PoseLandmarkerResult`) per variant as JSON, (3) add a regression test in a new `assessor`-adjacent eval harness that replays each fixture through `WarriorIIAssessor.assess` and asserts the expected `VerdictCode`, per feature-spec.md Section 13's eval-harness intent. Do not fabricate synthetic landmark data as a substitute — it wouldn't validate anything about the real MediaPipe output this phase is supposed to be checking.

  **New placeholder needing tuning/wiring**, not sourced from feature-spec.md: `HARDCODED_FRONT_LEG = Side.LEFT` in `MainActivity.kt` — no routine/step data exists yet to say which leg is forward, so it's hardcoded until Phase 5+ (Browse IA / Workout Mode) provides it.

  **New placeholder needing real content**, not sourced from feature-spec.md: `voice/CueTemplates.kt` — a minimal per-`VerdictCode` phrase map so the real pipeline has something to speak. feature-spec.md Section 9.2 calls for a proper "template-first language" library; that's Phase 7's job. Replace wholesale then, don't tune these wordings in place.

  **Threading note:** `PoseEstimator.estimate`'s `onResult` fires off the calling thread (MediaPipe's internal worker thread for the real implementation). `WarriorIIAssessor`/`CoachAgent` are pure computation so this is safe, and the one UI mutation (`fpsState`) already goes through `runOnUiThread`, unchanged from Phase 0.

- **Phase 5: Content model + Browse IA (Home / Category Browse / Routine Detail), plus Setup Tips built early.** Added `content/` package (`ContentModel.kt`: `Category`/`Routine`/`RoutineStep`/`Pose` matching feature-spec.md Section 4.1-4.3 exactly) with a pure-Kotlin JSON layer (`ContentJsonParser.kt`: `parseContentBundle(json, resolveDrawable)`, no Android imports, drawable-name→`@DrawableRes Int` resolution injected by the caller) and `ContentRepository.kt` (`InMemoryContentRepository` pure/testable + `loadContentRepositoryFromAssets()` as the one Android-coupled edge that reads `assets/content.json`). Bundled `app/src/main/assets/content.json` with the MVP taxonomy (5 categories per Section 3.1; the single `foundations_full_body` Routine per Section 3.2, referenced from both `full_body` and `legs_balance`) and all 3 MVP poses. 11 new JUnit tests (`ContentJsonParserTest`, `ContentRepositoryTest`) written from Given/When/Then before the implementation; 59/59 total across the suite on both `testDevPhoneDebugUnitTest` and `testIqooDebugUnitTest`.

  Built the four Composable screens as standalone, stateless functions (data + lambdas in, no ViewModel/nav wiring — matches the codebase's existing seam-based testing pattern, and there's no `WorkoutSessionState`/session store yet for a ViewModel to own): `ui/home/HomeScreen.kt` (US-1), `ui/category/CategoryBrowseScreen.kt` (US-2), `ui/routinedetail/RoutineDetailScreen.kt` (US-3), and `ui/setup/SetupTipsScreen.kt` — built now per this phase's explicit instruction even though Setup Tips isn't reached in navigation until Phase 6.

  **Real content-model finding, not a placeholder:** `foundations_full_body` (the only MVP routine) currently resolves `isCoachingSupported = false`. feature-spec.md Section 4.2 defines this as true only when every pose in the sequence has an Assessor rule set; Tree Pose and Chair Pose don't have one yet (only Warrior II, from Phase 1). This is the parser computing the field correctly from real per-pose `hasAssessorRuleSet` data, not a bug — Category Browse and Routine Detail both render their "Preview only" / disabled-CTA states accordingly. This will flip to `true` automatically once Tree/Chair rule sets land (a later, currently-unscheduled phase per the Section 7 phase table, which only names Warrior II).

  **Doc-vs-assets deviation, flagged not silently patched:** the task instructions referenced `docs/ux/pose-picker.md` for Category Browse/Routine Detail/Setup Tips; no such file exists. `docs/ux/` instead holds named Stitch export folders (`home_the_invitation`, `workout_picker_choose_your_practice`, `setup_preparing_your_space`, plus live-coaching/summary/memory exports for later phases) and a `DESIGN.md` token/component doc. Mapped: Home → `home_the_invitation`, Category Browse → `workout_picker_choose_your_practice`, Setup Tips → `setup_preparing_your_space`. **No Stitch export exists for Routine Detail at all** — built against `DESIGN.md`'s general system (cards, pill CTAs, tonal surfaces) instead, matching the same card language as the practice-picker export for visual consistency. Whoever runs a docs pass next should either add a Routine Detail export or fold this note into feature-spec.md so future agents don't go looking for a nonexistent file.

  **Placeholder needing real assets, flagged not buried inline:** every `iconRes`/`coverImageRes`/`thumbnailRes` in `content.json` points at one generic vector placeholder (`res/drawable/ic_placeholder.xml`) — no real category icons, routine covers, or pose thumbnails exist yet. Swap wholesale when real art/photography is available; don't hand-tune the placeholder.

  **Placeholder needing real assets, flagged not buried inline:** `ui/theme/Type.kt`'s `MiraType` approximates the Stitch exports' Playfair Display / DM Sans pairing with `FontFamily.Serif`/`FontFamily.SansSerif` — the real Google Fonts `.ttf` files weren't fetchable from this environment. Bundle the real fonts as assets (or wire up downloadable Fonts) before this ships.

  **Design-token scope decision:** added `ui/theme/MiraTokens.kt` (`MiraColors`/`MiraSpacing`/`MiraRadius`) as a standalone object rather than rewriting `Theme.kt`'s M3 `ColorScheme`/`Typography` — Mira's palette (near-black-green primary, editorial serif headlines) doesn't map cleanly onto Material3 roles, and this way Phase 0-4's existing `MiraAITheme`/`Purple40` scheme (still used by `MainActivity`'s camera-pipeline screen) is untouched. All four new screens read directly from `MiraColors`/`MiraSpacing`/`MiraType`/`MiraRadius`.

  **Scope notes for later phases:**
  - No navigation graph wires these four screens together yet, and `MainActivity` still boots straight into the Phase 4 camera-pipeline demo screen — intentional: real navigation needs Language Selection, Framing Assistant, and the Workout Mode Player to exist first (Phase 6+ per the phase table), and CLAUDE.md says not to re-architect completed phases. Added `androidx-navigation-compose` as a dependency now so Phase 6 doesn't need a separate dependency-wiring pass, but it isn't used by any code yet.
  - `RoutineDetailScreen`'s "Start Workout" CTA and `SetupTipsScreen`'s "I'm ready" CTA both currently take a bare `onClick` lambda with no navigation/session behavior wired — that's Phase 6 (`WorkoutSessionState` + player) territory.
  - `HomeScreen`'s "Continue last routine" card and "Recommended for you" section both take their data as plain parameters (`lastRoutine: Routine?`, `recommendedRoutines: List<Routine>>`) — no session-history read yet, since Room/local storage doesn't exist until Phase 9. Callers should pass `null`/`emptyList()` until then.

- **Phase 5 addendum: temporary navigation for on-device review of the Phase 5 screens.** Phase 5's Home/Category Browse/Routine Detail/Setup Tips screens were real and tested but unreachable — `MainActivity` still booted straight into the Phase 4 camera-pipeline demo, so there was no way to actually see them on-device. Added a `NavHost` (`androidx-navigation-compose`, already a declared-but-unused dependency from Phase 5) directly in `MainActivity.kt` wiring `Home -> Category Browse -> Routine Detail -> Setup Tips`, with a `HomeScreen` overlay button labeled "Camera Demo (temp)" — deliberately unstyled (plain Material `Button` defaults, red/white, not `MiraColors`) so it reads unmistakably as scaffolding — that routes into the unchanged Phase 4 `Phase0Screen` composable.

  **THIS IS TEMPORARY AND WILL BE REPLACED, NOT EXTENDED.** It stops at Setup Tips on purpose: `SetupTipsScreen`'s "I'm ready" button shows a toast ("Framing Assistant + Player aren't built yet — Phase 6") instead of navigating further, since Language Selection, Framing Assistant, the Workout Mode Player, and the `hasSeenSetupTips` skip flag are all Phase 6+ per build-architecture.md Section 7. `HomeScreen`'s Freestyle hero card similarly toasts instead of navigating (Freestyle is Phase 8). When Phase 6 builds the real navigation graph (with `WorkoutSessionState`-driven routing, not toast stubs), it should replace this `MiraNavHost`/`Routes` object wholesale rather than growing routes onto it — the temp camera-demo route and its button should be deleted at that point, not carried forward.

  No new tests: this is UI wiring with no new business logic (route table + toast stubs), consistent with Phase 5's screens already being covered by their own composables' existing coverage and the `content/` package's 11 tests. 59/59 tests still pass on both flavors; `assembleDevPhoneDebug` builds clean.

- **Phase 6: Framing Assistant (US-5), Workout Mode Player (US-6/Section 8), Session Summary (US-7).** Added the Routine Sequencer + Step Phase FSM as a new pure-Kotlin harness piece, deliberately separate from `CoachAgent` (two brains: `WorkoutSessionEngine` decides *what step/phase we're in and for how long*; `CoachAgent` decides *what to say*) — `agent/WorkoutSessionState.kt` (feature-spec.md Section 4.4, transcribed field-for-field), `agent/WorkoutStepExpander.kt` (`BOTH` side → two steps, per RoutineStep's doc comment), `agent/WorkoutSessionEngine.kt` (hold-timer wiring via Phase 1's `HoldTimer`, Section 8.6 rest windows, Section 8.7 early-end), `agent/FramingGate.kt` (US-5's confidence-gated auto-advance), `agent/SessionSummary.kt`/`StepResult` (US-7's per-pose results + next-focus line), and `agent/WorkoutThresholds.kt` (placeholder framing-confidence/sustain constants + the two spec-given rest windows, 5s/3s). 16 new JUnit tests (`FramingGateTest`, `WorkoutStepExpanderTest`, `WorkoutSessionEngineTest`), written from Given/When/Then against feature-spec.md Sections 8.2/8.3/8.6/8.7 and US-7; 75/75 total across the suite on both `testDevPhoneDebugUnitTest` and `testIqooDebugUnitTest`.

  Built three new Composable screens — `ui/framing/FramingAssistantScreen.kt` (US-5, matching docs/ux/live_coaching_framing_guide's "Fix your framing" variant), `ui/player/WorkoutPlayerScreen.kt` (Section 8.1's exact top-to-bottom layout: top bar, camera stage + confidence dot, hold-progress ring, cue caption bar, bottom pause/end controls, plus Section 8.4's non-blocking confidence-recovery banner), and `ui/summary/SessionSummaryScreen.kt` (US-7, matching docs/ux/session_summary_a_gentle_close's stat-row + closing-line layout) — then wired real navigation in `MainActivity.kt`, replacing the Phase 5-addendum toast stubs and deleting that phase's temporary camera-demo route/button wholesale (not extended, per that phase's own instruction): RoutineDetail → SetupTips → FramingAssistant → Player → Summary → Home, matching feature-spec.md Section 3's IA diagram.

  **Real camera wiring, not a mock-up:** `MainActivity` binds the Phase 3/4 `PoseEstimator` per-screen (`bindCameraForFraming` / `bindCameraForPlayer`), mirroring Phase 4's `handlePoseFrame` pattern. Framing computes a confidence signal from average landmark visibility and feeds `FramingGate`, auto-navigating to the Player once sustained-good per US-5. The Player runs `WarriorIIAssessor` + `CoachAgent` for `warrior_ii` steps (the only pose with a rule set) and feeds both the verdict and the resulting `CoachDecision` into `WorkoutSessionEngine.tick()`; the spoken cue line and the hold-progress ring both come from that one tick.

  **Known limitation, flagged not silently glossed over:** Tree Pose and Chair Pose still have no Assessor rule set (Phase 1 only covers Warrior II), so `WorkoutSessionEngine` ticks their hold timer with `verdict = null` — timer-only, no live form-gating, and their Session Summary result always reads "Good form." This means `foundations_full_body`'s `isCoachingSupported` is still `false` (per Phase 5's finding) and its real "Start Workout" CTA stays disabled; the full Setup→Framing→Player→Summary chain built this phase is verified via the Framing/Player screens' own preview composables and the engine's unit tests, not an on-device run through the disabled CTA. Whoever adds Tree/Chair rule sets should re-verify the full chain on-device at that point.

  ~~**Known gap:** Section 8.1 item 2's skeleton/angle overlay is not drawn on the Player's camera stage.~~ **Fixed in the Phase 6 follow-up below.**

  **Scope notes:** Skip-pose (P1, F18) is not implemented — the Player's bottom controls are Pause/Resume + End Workout only, per Section 8.1's "Skip (secondary, P1)" priority marking. Rep-counting (Section 8.5) is not implemented — the MVP's only content (`content.json`) has all three poses as `trackingMode: HOLD`, so there was nothing to exercise it against; `WorkoutSessionState.repCount` exists per the spec's data model but is always `0`.

- **Phase 6 follow-up: skeleton/angle overlay (F11) + a real coachable routine.** Two items closed against Phase 6's flagged gaps, not a new phase.

  **Fixed — skeleton + angle overlay.** Added `WarriorIIAssessor.frontKneeAngleDeg(frame, frontLeg)` (2 new JUnit tests — returns the target angle for the textbook pose fixture, null when the knee landmark is missing; 77/77 total across the suite on both flavors) alongside the existing `assess()`, so the one joint angle the overlay highlights comes from the same rule engine, not a UI-side recomputation. Added `ui/components/PoseOverlay.kt` — a Compose `Canvas` drawing thin `color.accent` (feature-spec.md Section 7, added as `MiraColors.accent`) lines between the 10 tracked joints plus a live `"92°"`-style label at the highlighted joint — and wired it into both `FramingAssistantScreen` and `WorkoutPlayerScreen` over the camera preview. `MainActivity` now publishes the live `PoseFrame` (and, for Warrior II steps, the front-knee angle) to Compose state from the same per-frame handlers that already drive the Assessor/CoachAgent/engine tick, so the overlay reads real per-frame landmark data, not a mock.

  **Not verified on-device — flagged, not claimed.** This sandboxed environment has no attached device or working `adb` session (`adb devices` did not return), so the overlay's real-time tracking and the new routine's on-device playthrough could not actually be confirmed here, unlike Phase 0's on-device smoke test. Compile (`compileDevPhoneDebugKotlin`/`compileIqooDebugKotlin`) and the full unit suite both pass, and the overlay's coordinate mapping relies on the same normalized-and-mirrored landmark space `MediaPipePoseEstimator` already produces (see its class doc) — but **whoever next has device access must run this before treating it as demo-ready**: confirm the skeleton actually sits on the body in real time (not offset/mirrored wrong) on both Framing and Player screens, and confirm "Warrior II — Quick Practice" plays start-to-finish into Summary.

  **Fixed — a real, clickable "Start Workout" end to end.** Added `warrior_ii_quick_practice` to `content.json`: Warrior II only (`BOTH` sides, 20s target), referenced from both `full_body` and `legs_balance` categories' `routineIds`. Since `isCoachingSupported` is computed from each pose's `hasAssessorRuleSet` (Phase 5's parser logic, untouched), this routine resolves to `true` automatically — no parser or content-model code changed. `foundations_full_body` is deliberately left exactly as-is: still `isCoachingSupported = false`, still showing "Preview only" with a disabled CTA per US-2 — that state is correct, not a bug, since Tree/Chair still have no rule set.

  **Still deferred, not touched:** Tree Pose and Chair Pose Assessor rule sets remain unbuilt, same known limitation as above — this follow-up explicitly did not build them (per instruction). `foundations_full_body` stays preview-only until that separately-tracked gap is closed.

- **Phase 6 follow-up 2: on-device bug reports fixed ahead of Phase 7.** The user ran "Warrior II — Quick Practice" on-device (vivo I2501) and reported two issues, both fixed as small, targeted changes rather than folded into Phase 7's scope:

  **Fixed — no visual reference for the target pose.** The Player only ever showed the user's *own* live skeleton (Phase 6 follow-up 1's overlay) plus spoken cues — nothing showed what the pose is actually supposed to look like. Added `ui/components/PoseReferenceDiagram.kt`: a small static stick-figure card (hand-authored joint coordinates, not derived from a photo/illustration asset — none exist yet, same placeholder-art gap flagged in Phase 5) rendered in the top-right corner of `WorkoutPlayerScreen` over the camera stage. Currently hardcoded to the Warrior-II-left-front-leg shape since that's the only pose with rule-set/routine support; **needs generalizing to a per-pose reference (keyed off `poseId`/`side`) once a second pose gets an Assessor rule set**, flagged here rather than guessed now.

  **Fixed — screen would dim/lock mid-workout.** Neither `WorkoutPlayerScreen` nor `FramingAssistantScreen` kept the screen awake, so Android's normal display timeout could black out the screen mid-hold with no free hand to tap it awake. Both screens now set `view.keepScreenOn = true` via a `DisposableEffect` (Compose `LocalView`) for as long as they're composed, clearing it on dispose — no manifest/window-flag changes, no effect on any other screen.

  No new unit tests: both are UI-only (a static Canvas diagram, a view property toggle), consistent with this codebase's existing screens being covered by their own composables rather than screenshot/UI tests. Verified via `./gradlew build` + `installDevPhoneDebug` on-device by the user.

- **Phase 7: Voice output.** Implemented the `TTSProvider` composition named in build-architecture.md Section 2.1 ("Piper as primary, Android system TTS as explicit fallback — not silent default"): `voice/PiperTTSProvider.kt` and `voice/FallbackTTSProvider.kt`, bound as the `TTSProvider` Koin singleton in both flavors' `AiModule.kt` (`FallbackTTSProvider(primary = PiperTTSProvider, fallback = SystemTTSProvider, onFallback = { Log.w(...) })`). Template lookup keyed to `VerdictCode`/`CoachIntent` (feature-spec.md Section 10.3) already existed from Phase 4 (`voice/CueTemplates.kt`, called from `MainActivity`'s `CoachIntent` dispatch) and needed no interface changes — only the delivery path underneath it changed. 4 new JUnit tests (`FallbackTTSProviderTest.kt`). Pure-Kotlin, no Android imports in the provider composition itself (only the concrete `Piper`/`SystemTTSProvider` leaf classes take a `Context`, matching `SystemTTSProvider`'s existing pattern). Full test suite + `./gradlew build` pass and on-device install (`installDevPhoneDebug`) succeeded, confirmed by the user.

  **Honest limitation, not a silent stub:** `PiperTTSProvider.isAvailable` is hardcoded `false` — there is no bundled Piper `.onnx` voice model or ONNX Runtime/`sherpa-onnx` native dependency in this repo. No such model was fetchable from this sandboxed environment, and picking a specific runtime binding (model size vs. quality vs. APK size) is a real product decision, not something to guess. Every `speak()` call today therefore throws `PiperUnavailableException` internally and is caught by `FallbackTTSProvider`, which logs a warning (`Mira.TTS` tag) and speaks via `SystemTTSProvider` instead — so today's behavior is unchanged from before this phase (system TTS speaks everything), but now it goes through the documented interface/fallback path instead of bypassing it. **Whoever adds the real Piper model next:** replace `PiperTTSProvider.isAvailable`/`speak()`'s body only — `FallbackTTSProvider`, the Koin bindings, and every call site (`MainActivity`, `CueTemplates`) need zero changes.

  **Real architecture-violation fixed in passing, not scope creep:** `MainActivity` was calling a directly-constructed `SpeechCoach` (raw Android `TextToSpeech` + its own redundant 4s cooldown) instead of the Phase 3 `TTSProvider` interface — a violation of CLAUDE.md's "All CV/LLM/STT/TTS access goes through the interfaces... never call a model API directly from UI or agent code." This was the exact wiring Phase 7 needed to touch anyway (the fallback composition has to sit where `SpeechCoach` sat), so `MainActivity` now injects `TTSProvider` via Koin and calls `ttsProvider.speak(line, Lang.EN)` directly; `voice/SpeechCoach.kt` was deleted (fully unused after the rewire, not deprecated-in-place). Note: `SpeechCoach`'s own 4000ms cooldown is gone along with it — `CoachAgent`'s `CUE_COOLDOWN_MS` (8000ms, Phase 2) already gates cue frequency, so the old dual-cooldown was redundant and could have silently eaten cues `CoachAgent` had decided to speak. No new bug introduced; a pre-existing latent one removed.

  **Not done this phase, deferred at the time, now partly closed (see Phase 7 follow-up below):** `voice/CueTemplates.kt`'s wording was still Phase 4's placeholder phrasing when this phase landed — only the delivery mechanism changed here, not the content.

- **Phase 7 follow-up: varied, natural coaching language + golden-form UI.** The user ran the Warrior II routine on-device and reported the coach repeating the exact same line ("I still can't see you — step back into frame.") every cooldown window while out of frame, which read as robotic, and asked for a visual "you've got it" signal plus a spoken rest/switch-sides transition instead of a silent on-screen label.

  **Fixed — repeated cues no longer say the identical line.** `CoachDecision` (`agent/CoachDecision.kt`) gained a `repeatIndex: Int` field — how many consecutive times the same `VerdictCode` has now been cued back-to-back, tracked in `CoachAgent` for both the regular issue path (`uncorrectedCount`) and safety overrides (new `safetyRepeatCount`, mirroring the same pattern). `voice/CueTemplates.kt` was rewritten so every code/escalation pair maps to a *list* of 2-3 natural phrasings instead of one fixed line; `forIssue`/`forSafetyOverride`/`forConfirmImprovement` pick a variant via `repeatIndex.mod(variants.size)` — deterministic (same repeatIndex always gives the same line, so it's unit-testable) and rotating (consecutive repeats cycle through different wording rather than repeating verbatim). `voice/CueTemplatesTest.kt` (5 new tests) covers variation, determinism, and cycling. This closes the wording gap the Phase 4/7 entries above flagged as deferred, for variety — the actual phrase content is still placeholder-quality English-only copy, not a professional coaching-language pass.

  **Fixed — a natural spoken line when a hold finishes, not just a silent "Switch sides" label.** `MainActivity.handlePlayerFrame` now detects the phase transition into `SessionPhase.REST` (feature-spec.md Section 8.6) and speaks one of `CueTemplates.forStepComplete(isSwitchingSides, variantSeed)` — different phrasing depending on whether the next step is the other side of the same pose vs. a genuinely new pose (`isSwitchingSides` is computed by comparing the just-finished step's `poseId`/`side` to `WorkoutSessionEngine.currentStep()` after the tick, since the engine has already advanced by the time `REST` shows up in the returned state).

  **Fixed — golden hue when form is correct.** Added `GoldenFormGlow` (private composable in `ui/player/WorkoutPlayerScreen.kt`): a soft gold radial-gradient wash + border over the camera stage, cross-faded in/out over 400ms via `animateFloatAsState` (so it doesn't flicker on single noisy frames), shown whenever `isGoodForm && !isResting && !isPaused`. `isGoodForm` is a new plain `Boolean` (`verdict?.verdictCode == VerdictCode.GOOD_FORM`, computed per-frame in `MainActivity.handlePlayerFrame`, exposed via `playerIsGoodForm()`) — deliberately not reusing `WorkoutSessionEngine`'s `HOLDING` phase, because `HOLDING` also covers non-critical issues (e.g. `ARMS_NOT_LEVEL`) that still let the hold timer run; the golden glow needed the stricter "actually correct form" signal instead.

  No new tests for the two UI-only pieces (golden glow, rest-transition trigger wiring in `MainActivity`), consistent with this codebase's pattern of covering pure logic (`CueTemplates`, `CoachAgent`) with JUnit and leaving Activity/Compose wiring to on-device verification. **Not verified on-device by the agent** (no attached device in this environment) — flagged, not claimed; ran `./gradlew build`/tests only. Whoever has device access next should confirm: the "I still can't see you" phrasing actually varies across repeats, the rest/switch-sides line is audible and timed right after a hold completes, and the golden glow appears/fades cleanly without flicker.

- **Phase 8: Freestyle — STT wiring, ActionSchema classifier, harness routing, orb/caption UI.** Added `agent/freestyle/ActionSchemaClassifier.kt` (pure Kotlin, implements `voice.LLMProvider`) — a keyword classifier over feature-spec.md Section 4.5's fixed 8-value `ActionSchema` enum, satisfying Section 12.5's "lightweight separate classifier" fallback option rather than an on-device SLM (no such model/runtime is available in this environment, same category of gap as Phase 7's Piper voice model). Because `ActionSchema` is a closed Kotlin enum, the classifier structurally cannot return a value outside the schema — unmatched utterances fall back to `ANSWER_SMALLTALK` by construction. Added `agent/freestyle/FreestyleHarness.kt` — the "harness routing" half (build-architecture.md Section 11.1 item 11): builds `AgentPrompt` with the always-full `allowedActions` set, invokes the `LLMProvider`, defensively re-validates the response against the allowed set (belt-and-suspenders for a future SLM-backed provider), and resolves `START_WORKOUT`/`SUGGEST_WARMUP` to a concrete routine id (`context.lastRoutineId` if present, else `warrior_ii_quick_practice` — the only `isCoachingSupported == true` routine per Phase 6 follow-up). 14 new JUnit tests: `ActionSchemaClassifierTest.kt` (one test per intent phrase for all 8 `ActionSchema` values, plus an explicit out-of-schema-utterance fallback test, per this phase's instruction) and `FreestyleHarnessTest.kt` (routine-id resolution, non-workout actions left unresolved, allowed-action-set passthrough). 101/101 JUnit tests pass on both `testDevPhoneDebugUnitTest` and `testIqooDebugUnitTest`; `assembleDevPhoneDebug` builds clean.

  Built `ui/freestyle/FreestyleScreen.kt` — the orb + caption screen per US-8's acceptance criteria ("show only the orb + live caption... no buttons except a small mic-mute affordance"): a breathing-pulse gold orb (`Canvas` + `rememberInfiniteTransition`, pulse period varies by `OrbState` — faster while THINKING/SPEAKING, slower while LISTENING/GREETING) plus caption text, a mic-mute toggle, and a back affordance (needed to leave the screen — every other screen in the app has one, not spec-forbidden).

  **Doc gap found, not silently guessed around — same pattern as Phase 5's Routine Detail:** the task instructions point at `docs/ux/freestyle.md`; no such file exists anywhere in `docs/ux/`. Built instead against `docs/ux/DESIGN.md`'s "Coach Speaking Indicator" component spec ("a subtle, breathing pulse animation using the Soft Gold color... a single glowing ring"). Whoever runs a docs pass next should add a real Stitch export for Freestyle or fold this note into feature-spec.md.

  **Wired end-to-end in `MainActivity.kt`:** `HomeScreen`'s Freestyle hero card now navigates to a real `Routes.FREESTYLE` screen instead of Phase 5-addendum's toast stub. `startFreestyle()` speaks an unprompted greeting (US-8: "Mira greets first, unprompted") built from the same hardcoded `SessionContext` defaults `HomeScreen` already uses (no real time-of-day/last-routine history until Phase 9 — flagged, not new scope), then calls `sttProvider.startListening`. `handleFreestyleTranscript()` runs the STT transcript through `FreestyleHarness.resolve()` (via `lifecycleScope.launch`, since `LLMProvider.complete` is suspend), speaks the resulting `spokenLine` through the existing `TTSProvider`, and on `START_WORKOUT`/`SUGGEST_WARMUP` sets a resolved-`Routine` state that a `LaunchedEffect` in the `FREESTYLE` composable picks up to navigate into `Setup Tips -> Framing -> Player` — per Section 8.8, Freestyle does not skip the Framing gate, matching the Browse path's own handoff exactly. Bound `ActionSchemaClassifier` as the `LLMProvider` Koin singleton in both flavors' `AiModule.kt`, replacing `StubLLMProvider` (which is now unused dead code — left in place as a documented placeholder pattern other providers still follow, not deleted, since no instruction called for a provider-file cleanup pass).

  **Honest limitation, not a silent stub — same pattern as Phase 7's Piper gap:** `STTProvider` is still bound to `StubSTTProvider()` in both flavors, which never invokes its `onResult` callback (real Whisper-Tiny wiring needs a bundled model/runtime not fetchable in this environment). This means the wiring above — `sttProvider.startListening { ... }` through to `FreestyleHarness` — is real and unit-testable at the classifier/harness level, but **cannot be exercised end-to-end on-device today**: a user can open Freestyle and hear the greeting, but nothing currently produces a transcript to classify. Swapping in a real `STTProvider` implementation is a drop-in change (same interface, same call site) once a model/runtime is available — no classifier, harness, or UI code should need to change.

  **Not done this phase, explicitly out of scope per the task instructions:** US-9 Memory Graph and the `Fact` consolidation function are Phase 10, not touched here. The Freestyle screen's `SessionContext.relevantFacts` is always passed as `emptyList()` since no `Fact` repository exists yet — the proactive-suggestion-from-memory half of US-8's story ("using relevant Facts") has no real facts to draw on until then; the greeting today only varies by the hardcoded time-of-day/last-routine defaults, not by memory.

## In progress
(none)

## Not started
Everything else — see docs/build-architecture.md Section 7 phase table. Outstanding side items, still not picked up: the Phase 6 follow-up 1's on-device overlay-tracking verification, the deferred golden-fixture regression test (flagged under Phase 4), a real Piper voice model/runtime (flagged under Phase 7), a genuine professional coaching-language/wording pass for `CueTemplates` (Phase 7 follow-up only added variety, not final copy), a real Whisper-Tiny `STTProvider` implementation and a `docs/ux/freestyle.md` Stitch export (both flagged under Phase 8), and Phase 8's Freestyle flow not yet verified on-device (no attached device in this environment — confirm the greeting speaks, the orb pulse renders without flicker, and the Setup/Framing/Player handoff triggers once STT is real). Check build-architecture.md Section 7 before starting the next phase — don't assume.

**New phase flagged by the user 2026-08-29, to be scheduled after Phase 8 (Freestyle) — needs its own spec/architecture pass before implementation, not yet in build-architecture.md's Section 7 phase table:**

**"On-device speech planning + session memory" (working title, needs a real phase number once scheduled).** Two related problems reported from on-device use, bundled into one future phase since the fix is the same mechanism:

1. **Bug: TTS cuts off mid-sentence on verdict change.** When `CoachAgent`/the assessor emits a new verdict/cue while a previous line is still being spoken, the in-flight utterance is cut off mid-word instead of finishing or yielding gracefully. Reported as a real UX problem, not cosmetic — it reads as broken, not just abrupt.
2. **Feature ask: an on-device LLM (user specifically named Qwen, running on the iQOO's Snapdragon NPU) that plans what to say instead of picking from a fixed template list.** Idea, as described by the user: this small on-device model would (a) track what's currently being spoken and the current session/pose state, (b) know the session's goal, (c) generate/"invent" its own phrasing rather than only selecting `CueTemplates` variants, (d) write progress/messages to `memory/` as it goes so a later dashboard chatbot can answer "how did I do" questions from that session's real history, and (e) do all this with low enough latency to feel like natural, non-robotic speech (this is also meant to directly fix bug #1 above — a model that knows what's currently playing shouldn't step on itself).

   **Explicitly flagged as needing real design work before coding, not just implementation:** this is a bigger architectural addition than a typical phase — it means a new on-device LLM inference path (Qwen-on-NPU is a specific runtime/model choice, same category of decision as the still-unresolved Piper TTS runtime pick from Phase 7), a new interaction contract between this planner and the existing `CoachAgent`/`CueTemplates`/`TTSProvider` pieces (per CLAUDE.md, must go through the `LLMProvider` interface in build-architecture.md Section 2 — never call a model API directly from agent/UI code), and a `memory/` write path feeding a not-yet-built dashboard chatbot (which itself doesn't exist yet — Section 9/10's Room/memory-consolidation phases are still unscheduled per the note above). The user framed this as a demo differentiator, so it's high-priority to *plan* next, but should get its own feature-spec.md/build-architecture.md section (goal framing, ActionSchema-style bounds on what the planner can say/do, latency budget, fallback behavior when the NPU path is unavailable — mirroring `FallbackTTSProvider`'s explicit-fallback pattern from Phase 7) before it gets a phase number and Given/When/Then tests.